[pytest]
DJANGO_SETTINGS_MODULE = core.settings
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --tb=short
    --strict-markers
    --disable-warnings
    --reuse-db
    --nomigrations
    --html=test_report.html
    --self-contained-html
markers =
    unit: Unit tests for individual components
    integration: API and workflow tests that exercise Django routes and persisted state
    e2e: Reserved for true end-to-end tests across external boundaries
    performance: Performance and load tests
    functional: Tests that exercise a feature end to end in process
    slow: Tests that take a long time to run
    external: Tests that require external services
testpaths = .
filterwarnings =
    ignore::django.utils.deprecation.RemovedInDjango60Warning
    ignore::DeprecationWarning
