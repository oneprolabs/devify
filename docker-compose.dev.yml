# Distinct project name so the dev stack is isolated from the production
# stacks (which use project "devify"); without this both default to the
# directory name and bringing one up recreates the other's containers.
name: devify-dev

x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

services:
  devify-api:
    image: devify:latest
    container_name: devify-api-dev
    restart: unless-stopped
    logging: *default-logging
    build:
      context: .
      dockerfile: Dockerfile
      args:
        DEV_MODE: "1"
        APT_MIRROR_URL: https://mirrors.tuna.tsinghua.edu.cn/debian
        PIP_INDEX_URL: https://pypi.tuna.tsinghua.edu.cn/simple
        PIP_TRUSTED_HOST: pypi.tuna.tsinghua.edu.cn
    env_file:
      - ./.env.dev
    volumes:
      - ./data.dev/django/staticfiles:/opt/devify/core/staticfiles
      - ./data.dev/logs/api:/var/log/gunicorn
      - ./devify:/opt/devify
      - ./data.dev/haraka/emails:/opt/haraka/emails
      - ./data.dev/haraka/email_attachments:/opt/haraka/email_attachments
      - ./data.dev/email_attachments:/opt/email_attachments
      - ./docker/nginx/certs:/opt/devify/docker/nginx/certs
    networks:
      - devify_network
    depends_on:
      mysql:
        condition: service_healthy
    command: development
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:8000/health"]
      interval: 5s
      timeout: 5s
      retries: 3
      start_period: 60s

  devify-worker:
    image: devify:latest
    container_name: devify-worker-dev
    restart: unless-stopped
    stop_grace_period: 60s
    logging: *default-logging
    env_file:
      - ./.env.dev
    volumes:
      - ./cache.dev:/opt/cache
      - ./data.dev/logs/worker:/var/log/celery
      - ./devify:/opt/devify
      - ./data.dev/haraka/emails:/opt/haraka/emails
      - ./data.dev/haraka/email_attachments:/opt/haraka/email_attachments
      - ./data.dev/email_attachments:/opt/email_attachments
    networks:
      - devify_network
    depends_on:
      devify-api:
        condition: service_healthy
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    command: celery

  devify-scheduler:
    image: devify:latest
    container_name: devify-scheduler-dev
    restart: unless-stopped
    stop_grace_period: 30s
    logging: *default-logging
    env_file:
      - ./.env.dev
    volumes:
      - ./data.dev/logs/scheduler:/var/log/celery
      - ./devify:/opt/devify
    networks:
      - devify_network
    depends_on:
      devify-api:
        condition: service_healthy
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    command: celery-beat

  mysql:
    image: registry.cn-beijing.aliyuncs.com/oneprolabs/mariadb:11.6
    container_name: devify-mariadb-dev
    restart: unless-stopped
    logging: *default-logging
    env_file:
      - ./.env.dev
    volumes:
      - ./docker/mysql/etc/my.cnf:/etc/mysql/my.cnf
      - ./docker/mysql/initdb.d:/docker-entrypoint-initdb.d
      - ./docker/mysql/scripts:/opt/devify/scripts
      - ./data.dev/mysql/data:/var/lib/mysql
      - ./data.dev/logs/mysql:/var/log/mysql
    networks:
      - devify_network
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 30s

  redis:
    image: registry.cn-beijing.aliyuncs.com/oneprolabs/redis:alpine
    container_name: devify-redis-dev
    restart: unless-stopped
    logging: *default-logging
    command: redis-server
    volumes:
      - ./data.dev/redis:/data
      - ./data.dev/logs/redis:/var/log/redis
    networks:
      - devify_network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s

  flower:
    image: devify:latest
    container_name: devify-flower-dev
    restart: unless-stopped
    logging: *default-logging
    env_file:
      - ./.env.dev
    ports:
      - "${FLOWER_HOST_PORT:-5555}:5555"
    depends_on:
      devify-api:
        condition: service_started
      redis:
        condition: service_healthy
    command: flower
    networks:
      - devify_network

  haraka:
    image: registry.cn-beijing.aliyuncs.com/oneprolabs/haraka:latest
    container_name: devify-haraka-dev
    restart: unless-stopped
    logging: *default-logging
    env_file:
      - ./.env.dev
    ports:
      - "${HARAKA_SMTP_PORT:-25}:25"
    volumes:
      - ./data.dev/haraka/logs:/var/log/haraka
      - ./data.dev/haraka/emails:/opt/haraka/emails
      - ./docker/haraka/config/plugins.dev:/etc/haraka/config/plugins:ro
      - ./docker/haraka/config/host_list.dev:/etc/haraka/config/host_list:ro
      - ./docker/haraka/config/log.ini:/etc/haraka/config/log.ini:ro
      - ./docker/haraka/config/smtp.ini:/etc/haraka/config/smtp.ini:ro
      - ./docker/haraka/config/relay.ini:/etc/haraka/config/relay.ini:ro
      - ./docker/haraka/plugins/raw_email_saver.js:/etc/haraka/plugins/raw_email_saver.js:ro
    networks:
      - devify_network

  devify-ui:
    image: devify-ui:latest
    build:
      context: ./ui
      dockerfile: Dockerfile.dev
    container_name: devify-ui-dev
    restart: unless-stopped
    logging: *default-logging
    volumes:
      - ./ui/src:/app/src:ro
      - ./ui/public:/app/public:ro
      - ./ui/index.html:/app/index.html:ro
      - ./ui/vite.config.js:/app/vite.config.js:ro
      - ./ui/tailwind.config.js:/app/tailwind.config.js:ro
      - ./ui/postcss.config.js:/app/postcss.config.js:ro
      - ./ui/package.json:/app/package.json:ro
      - ./ui/node_modules:/app/node_modules
    networks:
      - devify_network
    depends_on:
      devify-api:
        condition: service_started

  nginx:
    image: registry.cn-beijing.aliyuncs.com/oneprolabs/nginx:latest
    container_name: devify-nginx-dev
    restart: unless-stopped
    logging: *default-logging
    volumes:
      - ./docker/nginx/default.dev.conf:/etc/nginx/conf.d/default.conf
      - ./data.dev/django/staticfiles:/staticfiles:ro
      - ./data.dev/email_attachments:/opt/email_attachments:ro
      - ./data.dev/logs/nginx:/var/log/nginx
    ports:
      - "${DJANGO_HOST_PORT:-8000}:80"
    networks:
      - devify_network
    depends_on:
      devify-api:
        condition: service_healthy
      devify-ui:
        condition: service_started
    healthcheck:
      test: ["CMD", "nginx", "-t"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

networks:
  devify_network:
    driver: bridge
