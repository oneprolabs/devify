[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["devify"]

[project]
name = "devify"
version = "0.1.0"
description = "Devify is an enterprise-grade AI conversation intelligence platform with an Apache-2.0 open core and a separately licensed billing module."
authors = [{name = "Ray Sun", email = "xiaoquqi@gmail.com"}]
readme = "README.md"
requires-python = ">=3.11"
# Root package metadata uses the repository's primary license file.
# Billing-related code is separately licensed; see LICENSES.md.
license = { file = "LICENSE" }
dependencies = [
    "django==5.1.4",
    "djangorestframework==3.15.2",
    "djangorestframework-simplejwt==5.3.1",
    "djangorestframework-camel-case==1.4.2",
    "django-allauth>=65.0.0",
    "dj-rest-auth[with_social]==7.0.1",
    "django-cors-headers>=4.3.1",
    "drf-spectacular",
    "python-dotenv",
    "dj-database-url==2.3.0",
    "mysqlclient==2.1.1",
    "celery==5.3.1",
    "django-celery-beat==2.7.0",
    "django-celery-results",
    "flower==2.0.1",
    "redis",
    "django-redis>=5.4.0",
    # LangGraph workflow orchestration
    "langgraph>=0.6.7,<0.7.0",
    "langgraph-checkpoint>=2.1.1,<3.0.0",
    "langgraph-checkpoint-redis>=0.1.1,<0.2.0",
    "gunicorn==20.1.0",
    "setuptools==81.0.0",
    "beautifulsoup4>=4.12.0",
    "flanker>=0.9.11",
    "email-validator==2.1.1",
    "Pillow>=10.0.0",
    # PDF text extraction and page rendering for invoice recognition.
    # Pure manylinux wheel, no system libraries required.
    "pypdfium2>=4.30.0",
    "python-magic>=0.4.27",
    "django-json-widget>=1.1.1",
    "PyYAML>=6.0",
    "google-auth>=2.23.0",
    "google-auth-oauthlib>=1.1.0",
    "requests>=2.31.0",
    "rapidfuzz>=3.0.0",
    "jira>=3.5.1",
    "lark-oapi==1.5.3",
    "json-repair>=0.25.0",
    "dj-stripe>=2.8.0",
    "stripe>=8.0.0,<15.0.0",
    "sentry-sdk[django]>=2.0.0",
    "agentcore-metering @ git+https://github.com/cloud2ai/agentcore-metering.git",
    "agentcore-task @ git+https://github.com/cloud2ai/agentcore-task.git",
    "agentcore-notifier @ git+https://github.com/cloud2ai/agentcore-notifier.git",
]

[project.optional-dependencies]
dev = [
    "ipdb==0.13.13",
    "ipython==8.29.0",
    "pytest>=8.0.0",
    "pytest-django>=4.9.0",
    "pytest-html>=4.1.0",
    "pytest-cov>=6.1.0",
    "pytest-mock>=3.14.0",
    "pytest-sugar>=1.0.0",
    "pytest-xdist>=3.0.0",
    "pytest-metadata>=3.1.0",
    "factory-boy>=3.3.1",
    "mock>=5.1.0",
    "faker>=33.1.0",
    "coverage>=7.6.8",
    "mkdocs>=1.6.1",
    "black>=24.0.0",
    "isort>=5.13.0",
    "flake8>=7.0.0",
    "mypy>=1.8.0",
    "django-stubs>=4.2.0",
]
test = [
    "pytest>=8.0.0",
    "pytest-django>=4.9.0",
    "pytest-cov>=6.1.0",
    "factory-boy>=3.3.1",
]


[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "core.settings"
testpaths = [
    "devify/accounts/tests",
    "devify/threadline/tests",
    "devify/relay/tests",
    "devify/expense/tests",
    "devify/billing/tests",
]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "-v",
    "--tb=short",
    "--strict-markers",
    "--disable-warnings",
    "--color=yes",
]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks tests as integration tests",
    "unit: marks tests as unit tests",
    "e2e: marks tests as end-to-end tests",
    "external: marks tests that require real third-party services",
    "functional: marks tests as functional tests",
    "html_parsing: marks tests related to HTML parsing functionality",
    "django_db: marks tests that require database access",
    "auth: marks tests related to authentication",
    "performance: marks tests as performance tests",
]
filterwarnings = [
    "ignore::DeprecationWarning",
    "ignore::PendingDeprecationWarning",
]

# Coverage configuration
[tool.coverage.run]
source = ["threadline", "relay"]
omit = [
    "*/tests/*",
    "*/migrations/*",
    "*/__pycache__/*",
    "*/venv/*",
    "*/env/*",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if self.debug:",
    "if settings.DEBUG",
    "raise AssertionError",
    "raise NotImplementedError",
    "if 0:",
    "if __name__ == .__main__.:",
    "class .*\\bProtocol\\):",
    "@(abc\\.)?abstractmethod",
]

[tool.coverage.html]
directory = "htmlcov"

# Note: Code quality tools (black, isort, mypy) configurations removed
# Install them separately if needed: pip install black isort mypy
